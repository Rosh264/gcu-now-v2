export default function QuickActions() {
  return (
    <div className="quick-actions">

      <h2>Quick Actions</h2>

      <button className="primary-btn">
        + Add Company
      </button>

      <button>
        Post Drive
      </button>

      <button>
        Export Report
      </button>

    </div>
  );
}